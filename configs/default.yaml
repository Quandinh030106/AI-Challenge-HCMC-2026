# Configuration for HCMC AI Challenge 2026
data:
  raw_dir: "data/raw"
  keyframes_dir: "data/keyframes"
  features_dir: "data/features"
  metadata_dir: "data/metadata"

models:
  clip_model: "google/siglip-so400m-patch14-384"
  vlm_model: "Qwen/Qwen3-VL-8B-Instruct"

search:
  top_k_videos: 10
  top_k_frames: 100
